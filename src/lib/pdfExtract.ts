// PDF extraction util disabled — extraction functionality removed per user request.
export async function extractPdfFromBuffer() {
  throw new Error('PDF extraction util is disabled in this deployment');
}
